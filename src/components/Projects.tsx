import { PageTransition } from "./PageTransition";

export function Projects() {
  return (
    <PageTransition>
      <section className="min-h-screen pt-32 px-5 md:px-12 lg:px-24">
        <h1 className="text-5xl font-bold">Projects</h1>
        <p className="mt-8 text-muted">Section in progress...</p>
      </section>
    </PageTransition>
  );
}
